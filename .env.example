# Hostname used when generating shareable invite links
APP_HOSTNAME=localhost

# Host port mapped to the frontend container (http://<APP_HOSTNAME>:<FRONTEND_PORT>)
FRONTEND_PORT=8080

# Uvicorn log level for the backend container
BACKEND_LOG_LEVEL=info
